# Issue tracker

This repository uses GitHub Issues for tracking work.

- Use `gh issue create` / `gh issue edit` for issue management.
- The issue tracker is the source of truth for incoming bugs, feature requests, and implementation tasks.
- PRs are not treated as a request surface for triage by default.
